version: '3'
services:
  mongodb:
    image: mongo:latest
    container_name: mongodb
    ports:
      - '27017:27017'
  nodeapp:
    build: .
    container_name: authentication-server
    ports:
      - '3001:3001'
    depends_on:
      - mongodb
